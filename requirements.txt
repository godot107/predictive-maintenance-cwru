# Runtime dependencies for the Streamlit app + PyTorch model.
#
# torch is resolved from the CPU wheel index so a fresh `pip install` works on
# CPU-only hosts (e.g. Streamlit Community Cloud) without pulling a multi-GB CUDA
# build. GPU users: install a CUDA build of torch *first* (see the README), and
# pip will keep it — the requirement below is already satisfied.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
scikit-learn>=1.0.0
torch>=2.0.0
matplotlib>=3.4.0
streamlit>=1.10.0
